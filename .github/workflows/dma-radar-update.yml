name: Update DMA Radar scan

on:
  schedule:
    - cron: "7 11 * * 1-5"    # 11:07 UTC = 4:37 PM IST - off the round-hour mark on purpose
  workflow_dispatch: {}

permissions:
  contents: write

defaults:
  run:
    working-directory: dma-radar

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: dma-radar/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scan
        run: python scripts/scan.py --sleep 0.6

      - name: Commit updated data
        run: |
          git config user.name "dma-radar-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/scan_results.json
          git diff --staged --quiet && echo "No changes to commit" || git commit -m "DMA radar scan update: $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
